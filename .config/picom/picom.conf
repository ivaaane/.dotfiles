backend = "glx";
vsync = false;

inactive-dim = 0.5;
focus-exclude = [
    "class_g = 'Rofi'",
    "class_g = 'Ibus-table'",
    "class_g = 'Dunst'"
];

#shadow = true;
#shadow-exclude = ["class_g = 'Polybar'"];
